# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environments
aivc_env/
venv/
.venv/
.venv311/
env/
artifacts/

# Large data files — never commit
*.h5ad
*.npy
*.pkl
*.pickle

# Model checkpoints — in S3, not git
models/
*.pt
*.pth
*.ckpt

# Keep output structure — figures and notebook are committed
outputs/v1.0/notebook/*_executed.ipynb

# Results sweep files (large)
results/v1.0/*.npy

# Runtime generated
training_log*.txt
test_split_info*.pt

# STRING PPI raw data
data/9606.protein.*.txt
data/9606.protein.*.txt.gz

# Jupyter checkpoints
.ipynb_checkpoints/

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs (optional — keep training logs in repo for reproducibility)
# training_output_*.log

# AWS credentials — never commit
.aws/
*.pem

# Local git identity reference (not for commit)
.gitconfig-check

# hardening layer
experiments/*/quarantine.pt
experiments/best/best.pt
.context.lock
artifacts/

# Phase 6.7: real-data fragments + peak-set artifacts (never committed;
# canonical references are SHA-256 hashes in .github/REAL_DATA_BLOCKERS.md)
data/raw/
data/peak_sets/*.tsv
data/peak_sets/*.meta.json
!data/peak_sets/README.md

# Phase 6.7b: real-data Multiome AnnData built from 10x + peak_set.
# Never committed; provenance hashes live in the sibling .meta.json and
# are echoed into the PR body + REAL_DATA_BLOCKERS.md appendix.
data/pbmc10k_multiome.h5ad
data/pbmc10k_multiome.h5ad.meta.json

# Phase 6.5a: Norman 2019 aligned AnnData + DE JSON + provenance sidecars.
data/norman2019_aligned.h5ad
data/norman2019_aligned.h5ad.meta.json
data/norman2019_aligned.h5ad.gene_intersection.txt
data/norman2019_top50_de.json

# Local experiment outputs
experiments/best/
experiments/test_*/
experiments/test_run/
tea_debug.log
wandb/

# Experiment + memory state — drifting JSON files that should not be tracked.
# Untracked via PR #35 (2026-04-27) after they forced rebase stashes during M1.
experiments/index.jsonl
memory/experiments.json
