torch==2.2.2
torch-geometric==2.7.0
torchvision==0.17.2
numpy==1.26.4
scipy>=1.11.0
scanpy==1.11.5
anndata>=0.10.0
POT>=0.9.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# HTTP client (for AIVCClient SDK)
requests>=2.31.0

# experiment tracking
wandb>=0.17.0,<0.19
mlflow>=2.12.0,<3.0
GitPython>=3.1.40
anthropic>=0.25.0

# memory layer
jinja2>=3.1.0,<4
pyyaml>=6.0

# cli
typer[all]>=0.12.0

# hardening
dvc>=3.0

# Genomic interval operations (peak overlap / harmonization)
pyranges>=0.0.129

# Genome coordinate liftOver (hg19 -> hg38 for Calderon)
pyliftover>=0.4

# Linear probe + CV (was transitive via scanpy; explicit per direct usage)
scikit-learn>=1.3
