[pytest]
testpaths = tests
markers =
    real_data: tests requiring real external datasets (download or local h5ad). Excluded from default run.
    slow: tests with wall time > 60s. Includes long-running real-data smoke tests.
addopts = -m "not real_data"
